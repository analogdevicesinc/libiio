#!/bin/sh

if [ "x${COVERITY_SCAN_PROJECT_NAME}" != "x" ] ; then exit 0; fi

cd $TRAVIS_BUILD_DIR/build
cmake -DENABLE_PACKAGING=ON ..
make
ls

cd $TRAVIS_BUILD_DIR/build_tar
cmake -DENABLE_PACKAGING=ON -DOSX_PACKAGE=OFF ..
make && make package
ls
